<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose paints every pixel, but the window background is what shows during
      the cold-start frame before the first composition. Leaving it at the
      framework default meant a flash of platform grey ahead of the plum UI.

      Bars are transparent because MainActivity goes edge-to-edge and the
      vendored PixelPlayStatusBarStyle drives their icon appearance from the
      colour scheme at runtime.
    -->
    <style name="AppTheme" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
